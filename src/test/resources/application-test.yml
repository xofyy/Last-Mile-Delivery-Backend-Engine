spring:
  config:
    activate:
      on-profile: test

resilience4j:
  ratelimiter:
    instances:
      orderApi:
        limitForPeriod: 5
        limitRefreshPeriod: 1s
        timeoutDuration: 0
